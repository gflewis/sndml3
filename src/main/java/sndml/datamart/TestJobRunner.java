package sndml.datamart;

/**
 * Used for JUnit Tests
 */
public class TestJobRunner extends JobConfigRunner {

	public TestJobRunner(ConnectionProfile profile, JobConfig config) {
		super(profile, config);
	}

}
